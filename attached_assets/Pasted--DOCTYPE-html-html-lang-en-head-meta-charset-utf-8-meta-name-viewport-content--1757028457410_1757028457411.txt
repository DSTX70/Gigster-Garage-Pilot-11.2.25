<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Gigster Garage</title>

  <!-- Brand typography -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Montserrat:wght@700;800&display=swap" rel="stylesheet">

  <style>
    :root{
      /* Approved palette (from your kit) */
      --blue-tint:  #66C7FF;
      --blue-shade: #006FCC;  /* deep accent */
      --teal-tint:  #66E0C7;
      --teal-shade: #008272;  /* primary CTA */
      --gray-tint:  #7F8F7F;  /* muted */
      --gray-shade: #1A241A;  /* deep text on light */
      --silver-tint:#E0E0E0;  /* dividers / cards */
      --silver-shade:#A0A0A0; /* low-contrast accents */

      --text-on-dark: #FFFFFF;
      --surface: #FFFFFF;

      /* Layout rhythm */
      --container-max: 1152px;
      --radius-xl: 16px;
      --radius-pill: 999px;
      --shadow-soft: 0 10px 25px rgba(0,0,0,.15);
    }

    /* Reset-ish */
    * { box-sizing: border-box; }
    html, body { height: 100%; }
    body{
      margin:0;
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji","Segoe UI Emoji","Segoe UI Symbol", sans-serif;
      color: var(--text-on-dark);
      /* Refined brand gradient: deep blue -> teal */
      background:
        linear-gradient(115deg, var(--blue-shade) 0%, #0B2B5A 20%, #0F4E78 45%, #0F7B70 70%, var(--teal-shade) 100%)
        fixed;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    .container{
      max-width: var(--container-max);
      margin: 0 auto;
      padding: clamp(20px, 4vw, 40px);
    }

    header{
      display: flex;
      align-items: center;
      gap: 14px;
    }
    .logo{
      width: 64px; height: 64px;
      display:block;
      /* keep whatever your current logo file is – no visual edits */
      border-radius: 12px; /* non-destructive: just prevents sharp artifacts on exported PNGs */
    }
    .brand-name{
      font-family: Montserrat, Inter, sans-serif;
      font-weight: 800;
      font-size: clamp(28px, 4vw, 36px);
      line-height: 1.1;
      letter-spacing: .2px;
    }

    .hero{
      display:grid;
      place-items:center;
      text-align:center;
      padding: clamp(32px, 8vw, 96px) 0;
    }
    .hero h1{
      font-family: Montserrat, Inter, sans-serif;
      font-weight: 800;
      font-size: clamp(40px, 8vw, 72px);
      line-height: 1.05;
      margin: 18px 0 10px;
      color: #fff;
      text-wrap: balance;
    }
    .tagline{
      font-weight: 600;
      letter-spacing: .3px;
      color: var(--teal-tint); /* soft, on-brand accent */
      font-size: clamp(16px, 2.2vw, 22px);
      margin-bottom: 18px;
    }
    .lede{
      color: rgba(255,255,255,.90);
      font-size: clamp(16px, 2.2vw, 20px);
      line-height: 1.6;
      max-width: 820px;
      margin: 0 auto 28px;
      text-wrap: pretty;
    }

    .cta-row{
      display:flex;
      gap: 14px;
      justify-content: center;
      flex-wrap: wrap;
      margin-top: 8px;
    }
    .btn{
      appearance:none; border:0; cursor:pointer;
      font-weight: 700; letter-spacing:.15px;
      padding: 14px 22px;
      border-radius: var(--radius-pill);
      transition: transform .06s ease, box-shadow .25s ease, background-color .25s ease, color .25s ease, border-color .25s ease;
      will-change: transform;
      white-space: nowrap;
    }
    .btn:active{ transform: translateY(1px); }

    .btn-primary{
      background: var(--teal-shade);
      color:#fff;
      box-shadow: var(--shadow-soft);
    }
    .btn-primary:hover{ background: var(--blue-shade); }
    .btn-secondary{
      background: rgba(255,255,255,.12);
      color:#fff;
      border: 1px solid rgba(255,255,255,.28);
      backdrop-filter: saturate(120%) blur(6px);
    }
    .btn-secondary:hover{
      background: rgba(255,255,255,.18);
      border-color: rgba(255,255,255,.45);
    }

    .microcopy{
      margin-top: 10px;
      font-size: 14px;
      color: rgba(255,255,255,.85);
    }

    .cred-bar{
      margin-top: clamp(26px, 4vw, 40px);
      display:flex; justify-content:center; align-items:center; gap: 18px;
      flex-wrap: wrap;
      color: rgba(255,255,255,.92);
      font-size: 15px;
    }
    .cred-pill{
      display:flex; align-items:center; gap:10px;
      background: rgba(255,255,255,.12);
      border: 1px solid rgba(255,255,255,.18);
      padding: 10px 14px;
      border-radius: var(--radius-pill);
    }
    .check{
      width:18px; height:18px; flex:0 0 18px;
      color: var(--teal-tint);
    }

    /* Accessibility improvements */
    a.btn:focus, button.btn:focus{
      outline: 3px solid var(--blue-tint);
      outline-offset: 2px;
    }

    /* Small screens */
    @media (max-width: 560px){
      .cta-row{ flex-direction: column; }
      .cta-row .btn{ width: 100%; }
      header{ justify-content: center; }
    }
  </style>
</head>
<body>
  <div class="container">

    <!-- Header: uses your existing logo asset. Do not change file; swap only the src path if needed -->
    <header aria-label="Site header">
      <img class="logo" src="YOUR-CURRENT-LOGO-PATH.png" alt="Gigster Garage logo">
      <div class="brand-name">Gigster Garage</div>
    </header>

    <!-- Hero -->
    <section class="hero" aria-labelledby="hero-title">
      <h1 id="hero-title">Gigster Garage</h1>
      <div class="tagline">Smarter tools for bigger wins. From Garage to Global.</div>

      <p class="lede">
        Simplify your workflow with smart automation, task tracking, and effortless team collaboration —
        built for ambitious creators.
      </p>

      <div class="cta-row" role="group" aria-label="Primary actions">
        <a class="btn btn-primary" href="#get-started" aria-label="Get started free">Get Started Free →</a>
        <a class="btn btn-secondary" href="#sign-in" aria-label="Sign in">Sign In</a>
      </div>

      <div class="microcopy" aria-live="polite">
        Takes 30 seconds — no credit card required.
      </div>

      <!-- Credibility bar -->
      <div class="cred-bar" aria-label="Key benefits">
        <div class="cred-pill">
          <svg class="check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          Free forever
        </div>
        <div class="cred-pill">
          <svg class="check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          No card required
        </div>
        <div class="cred-pill">
          <svg class="check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          Team-ready
        </div>
      </div>
    </section>
  </div>
</body>
</html>