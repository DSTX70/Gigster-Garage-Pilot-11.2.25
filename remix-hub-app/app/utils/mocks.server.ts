const id=(p:string)=>p+'_'+Math.random().toString(36).slice(2,8); const iso=()=>new Date().toISOString(); export function mockCreateIntake(payload:any){return{ticket_id:id('TCK'),pod_guess:'Scaffolds/Starters',scope_stub_uri:'uri:///plans/scope_stub.md',received:payload,created_at:iso()}} export function mockCreatePlan(ticketId:string,body:any){return{plan_id:id('PLAN'),checklist_uri:'uri:///plans/checklist.md',ticket_id:ticketId,plan:body,created_at:iso()}} export function mockExecutePlan(planId:string,body:any){return{run_id:id('RUN'),status_uri:'uri:///runs/status.json',plan_id:planId,config:body,started_at:iso()}} export function mockGetReview(runId:string){return{mirrors:['Kickoff posted','CI green','Release notes drafted'],kpis:{on_time_milestone_rate:1.0,green_build_rate:1.0,'gm%':0.62,cost_per_req:0.003},decisions:['Proceed to next wave'],evidence_uri:'uri:///evidence/evidence.zip',run_id:runId,updated_at:iso()}}